import styled from "styled-components";
import { StyledCardBase } from "../styles/BaseStyles";

const StyledMediumCard = styled(StyledCardBase)`
  height: 380px;
  width: 360px;
`;

export default StyledMediumCard;
